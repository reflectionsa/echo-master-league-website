# ── Google Sheets ────────────────────────────────────────────────────────────
# Get your API key from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_SHEETS_API_KEY=your_google_sheets_api_key_here

# ── Discord OAuth ─────────────────────────────────────────────────────────────
# Application ID from https://discord.com/developers/applications (not a secret)
VITE_DISCORD_CLIENT_ID=1477115120759996667

# The URL Discord will redirect back to after login.
# For local dev:  http://localhost:3000/
# For production: https://reflectionsa.github.io/echo-master-league-website/
# Must be added to your Discord app's OAuth2 > Redirects list.
VITE_DISCORD_REDIRECT_URI=http://localhost:3000/

# ── Cloudflare Worker ─────────────────────────────────────────────────────────
# The deployed URL of the eml-discord-oauth worker, e.g.:
# https://eml-discord-oauth.<your-subdomain>.workers.dev
VITE_WORKER_URL=https://your-worker.your-subdomain.workers.dev

# ── Instructions ─────────────────────────────────────────────────────────────
# 1. Copy this file to .env  (never commit .env — it stays gitignored)
# 2. Fill in the values above.
# 3. Make sure Google Sheets API is enabled in your Google Cloud project.
# 4. The spreadsheets are already configured in config/sheets.js.
